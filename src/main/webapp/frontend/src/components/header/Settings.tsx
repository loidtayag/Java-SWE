import styled from "styled-components";

const Settings = () => {
  return (
    <Helper>
      <img src="/Hide.svg" alt="Settings" />
    </Helper>
  );
};

const Helper = styled.button`
  width: 2rem;
  height: auto;
`;

export default Settings;
